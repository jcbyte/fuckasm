{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-asm",
            "type": "shell",
            "command": "nasm -f elf64 main.asm -o main.o && ld main.o -o main",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
